apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: spark-minio-write-test
  namespace: lakehouse
spec:
  type: Python
  mode: cluster

  image: "k8s-lakehouse/spark:3.5.6-v2"
  imagePullPolicy: IfNotPresent

  mainApplicationFile: local:///opt/spark/work-dir/app/test_minio_write.py
  sparkVersion: 3.5.6
  sparkConf:
    "spark.hadoop.fs.s3a.endpoint": "http://minio-service.lakehouse.svc.cluster.local:9000"
    "spark.hadoop.fs.s3a.access.key": "H7Zg4e0ilw7E5dRPPEdh" # TODO: PARA PROD PRECISARÁ FICAR EM SECRETS
    "spark.hadoop.fs.s3a.secret.key": "RuMdGNRMeTW3FeUrA473WzRY4NfU8z4PPa0AeEIk" # TODO: PARA PROD PRECISARÁ FICAR EM SECRETS
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
  
  restartPolicy:
    type: Never
  
  driver:
    cores: 1
    memory: "512m"
    serviceAccount: spark-operator-spark
  
  executor:
    cores: 1
    instances: 2
    memory: "512m"